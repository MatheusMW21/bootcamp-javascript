//Map VS forEach

// Usando map

const array = [1, 2, 3, 4, 5];

array.map((item) => item * 2); // retorno: [2, 4, 6, 8, 10];

// Usando forEach

const arr = [1, 2, 3, 4, 5];

arr.forEach((item) => item * 2); // retorno : undefined

