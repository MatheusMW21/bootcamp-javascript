//entendendo as funções no JS
function soma (a, b) {
   // console.log(a + b); apenas para debugar o código, para checar se a função está sendo chamada.
    return a + b; // 8
}

soma(3, 5); //nessa linha chama a função.
